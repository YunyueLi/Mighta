# Cloudflare Workers + Static Assets config (modern unified format)
# Deploy: `npx wrangler deploy`
name = "mighta"
compatibility_date = "2025-01-01"

[assets]
directory = "./dist"
# SPA fallback — any unknown path serves /index.html with the React router taking over
not_found_handling = "single-page-application"
